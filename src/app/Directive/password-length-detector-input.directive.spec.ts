import { PasswordLengthDetectorInputDirective } from './password-length-detector-input.directive';

describe('PasswordLengthDetectorInputDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordLengthDetectorInputDirective();
    expect(directive).toBeTruthy();
  });
});
